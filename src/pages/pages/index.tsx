import 'shadcn-ui-lib/styles.css';
import type { NextPage } from 'next';
import { Card } from 'shadcn-ui-lib';

const Home: NextPage = () => {
  return (
    <main className="p-4">
      <Card className="p-4">Pages dir test page</Card>
    </main>
  );
};

export default Home;
